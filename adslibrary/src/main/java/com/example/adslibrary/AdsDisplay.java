package com.example.adslibrary;

import android.content.Context;
import android.os.Bundle;
import android.webkit.WebSettings;
import android.webkit.WebView;
import android.view.View;

/**
 * Created by isaacshrestha on 2/8/18.
 */

/*
public class AdsDisplay {

    public void DisplayAds(final View view) {



        WebView myWebView = (WebView) view.findViewById(R.id.my_web_view);
        WebSettings webSettings = myWebView.getSettings();
        webSettings.setJavaScriptEnabled(true);
        webSettings.setDomStorageEnabled(true);
        myWebView.loadUrl("http://www.ishrestha.com/metawear-ads/ads.php");
        myWebView.addJavascriptInterface(new WebAppInterface(this.getContext()), "Android" );


        //myWebView.addJavascriptInterface(new WebAppInterface(), "Android" );
        //The expected context parameter here is troubling ...
    }
}
*/
